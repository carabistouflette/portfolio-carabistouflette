// Plugin pour précharger les collections d'icônes
export default defineNuxtPlugin(() => {
    // Ce plugin ne contient pas de logique directe, car Nuxt Icon gère déjà le chargement des icônes
    // Mais en l'incluant comme fichier distinct, on s'assure que les collectons sont chargées correctement
    console.info('Icon collections initialized');
  });