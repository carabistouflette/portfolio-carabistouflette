// Middleware pour le serveur Nuxt qui garantit le bon fonctionnement des ressources statiques
export default defineEventHandler((event) => {
  // Ce middleware ne fait rien de spécial, il est juste là pour s'assurer 
  // que le dossier server/middleware existe
})